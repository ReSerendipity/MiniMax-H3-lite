# MM·H3 工作台 · 实现任务清单

> 依据 [docs/PRD.md](PRD.md) 与根目录 `.design.json` 拆解。按阶段推进，每项含验收标准。
> 原则：严格实现本清单与 PRD 内的功能，不做清单之外的事。

## 阶段 A：前端高保真升级（先行，无后端依赖）

| # | 任务 | 验收标准 |
|---|---|---|
| A1 | 根 `index.html` 由灰度线框升级为高保真：应用项目强调色（随展示壳模式切换：剧场红 `#b3261e` / 电视琥珀 `#b07612` / 放映机青绿 `#1d7873`，暗色调亮）替换灰色 `#3a3f4a` | 明暗双主题主色正确 |
| A2 | 清除线框残留：`.wf-hatch` 斜纹占位、状态栏「媒体/容器/分组」图例、预览区占位语义 | 无占位 / 线框字样残留 |
| A3 | 明暗双主题对比度与可读性复核 | 暗色下文本可读，无重叠 |
| A4 | 响应式（桌面 / 平板 / 手机）与可访问性复核 | 三端可用，键盘可达，设计质量 P0=0 |
| A5 | 设计系统定稿：`.canvas-meta.json` 中 `designSystemApproved` 置为 `true` | 审批标记生效 |

## 阶段 B：后端骨架（FastAPI + 数据模型）

| # | 任务 | 验收标准 |
|---|---|---|
| B1 | 初始化 FastAPI 后端，SQLite 建库，实现 §4.3 数据模型（Project / Shot / GenerationTask / Asset） | 建表与 CRUD 可用 |
| B2 | 项目 / 镜头 / 历史 API（PRD §8 草案） | 接口返回正确，数据按项目隔离 |
| B3 | 参考素材上传接口（返回 asset_id，格式 / 大小 / 上限校验） | 校验规则生效（≤12 项、音频须配图或视频） |
| B4 | 统一配置管理（模型路径 / 推理地址 / 端口 / 量化档位 / 最大并发），风格向兄弟项目对齐 | 配置集中可改 |
| B5 | 启动脚本：PowerShell 一键启动（前端静态服务 + 后端） | 一键拉起，端口不冲突 |

## 阶段 C：任务队列与推理接入

| # | 任务 | 验收标准 |
|---|---|---|
| C1 | 任务队列：状态机 pending → processing → completed / failed，超时与重试，失败原因记录 | 状态真实流转，错误透传 |
| C2 | 推理客户端：对接本地 H3-Base（diffusers 或 ComfyUI，按 PRD §6.2 择优） | 768P 音画一体生成成功 |
| C3 | 推理参数映射：模式 / 时长 / 宽高比 / 帧率 / 参考素材 → H3 输入 | 与模型规格对齐（4–15s、六比例、≤7000 字符提示词） |
| C4 | 生成结果落盘 assets/ + 资产记录 + 缩略帧提取 | 结果可回看可播放 |
| C5 | 前端「生成」改为真实请求：提交任务 → 轮询 → 结果帧回填舞台 | 完整闭环可用 |

## 阶段 D：历史库与多项目前端

| # | 任务 | 验收标准 |
|---|---|---|
| D1 | 前端项目列表：新建 / 重命名 / 删除 / 切换 | 切换不串数据 |
| D2 | 前端历史库：已完成镜头列表、预览、一键复用参数 / 提示词 | 可回看可复用 |

## 阶段 E：收尾

| # | 任务 | 验收标准 |
|---|---|---|
| E1 | 本地推理能力边界文案（Context-IR 轻量提示词优化、2K 占位不可用） | 用户理解能力边界 |
| E2 | 初始化 git 仓库 + .gitignore + 首次提交 | 版本可控 |
| E3 | 端到端验收（对照 PRD §9） | 全部通过 |
| E4 | 决定 `_archive/` 去留 | 明确归档策略 |

## 备注 / 待确认

- 推理框架最终选型（diffusers vs ComfyUI）与量化档位需结合落地环境显存确定（PRD §6.2–§6.3）。
- 2K（H3-Regenerate-2K）与官方 Context-IR 明确不做本地实现。
