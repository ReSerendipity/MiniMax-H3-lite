# 构建上下文排除项（减小镜像体积、避免把大权重/运行时数据打进镜像）
.git
.venv
node_modules
web-prototypes
model
data
uploads
outputs
.pytest_cache
# detect-secrets 基线元数据（随 .git 同类，不入包）
.secrets.baseline
htmlcov
__pycache__
*.pyc
*.db
_archive
_dl
scripts/*.ps1
logs/
backup/
# vendored ComfyUI 内核（GPL-3.0）：不随镜像分发，由 docker-compose 只读挂载提供。
# 取舍理由与后果见 docs/GPL_COMPLIANCE.md §2；改这里必须同步该文档。
comfy_kernel
